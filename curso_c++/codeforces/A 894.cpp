#include <bits/stdc++.h>

using namespace std;

int main(){
    string n; cin >> n;

    int len = n.size();
    for(int i = 0; i < len; ++i){
        if(n[i] == 'Q'){
            for(int j = i + 1; j < len; j++){
                if()
            }
        }
    }
}